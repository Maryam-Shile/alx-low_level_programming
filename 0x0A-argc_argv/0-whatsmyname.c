#include <stdio.h>
#include <stdlib.h>

/**
 * main -main argument of the function
 * @argc: argument count
 * @argv: arguments passed
 * Return: returns0 when successful
 */

int main(int argc, char **argv)
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
