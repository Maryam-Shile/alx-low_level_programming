#include "main.h"
#include <stdio.h>

/**
 * swap_int - check the code
 * Description: swaps integers using pointers
 * Return: Always 0.
 * @a: first pointer
 * @b: second pointer
 */

void swap_int(int *a, int *b)
{
	*a = int *b;
	*b = int *a;
}
